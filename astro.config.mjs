// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config

export default defineConfig({
  site: 'https://why0not.github.io', // Tu usuario de GitHub
  base: '/whynot',                   // El nombre del repositorio
});